# NFT 기반 창작물 플랫폼 로직 정리

## 1. 프로젝트 기술 스택

- **Frontend**: React / Vite
- **Backend**: Spring Boot
- **Database**: MariaDB
- **Blockchain / Smart Contract**: Solidity, Remix IDE, MetaMask, Hoodi 테스트넷
- **개발 환경**
  - DB(MariaDB)만 Docker 컨테이너로 실행
  - 프론트엔드와 백엔드는 개발 중 각자 로컬에서 실행

---

## 2. 현재 개발 환경 및 진행 상황

### 현재 구성
- 프론트엔드: 디자인 및 기본 화면 코드 작성 완료
- 백엔드: Spring Initializr를 이용해 Maven 기반 Spring Boot 프로젝트 생성 완료
- DB: Docker Compose로 MariaDB 컨테이너 실행 완료
- 백엔드와 DB 연결: 완료

### 현재 상태 요약
- Spring Boot 서버가 MariaDB에 정상 연결됨
- 아직 엔티티, 리포지토리, 실제 비즈니스 API는 미구현 상태
- 블록체인 연동도 아직 본격 구현 전 단계

---

## 3. 핵심 설계 원칙

### 인증 / 계정
- 회원가입/로그인과 지갑 연결은 분리한다.
- 로그인 방식은 **세션 기반 인증**으로 처리한다.
- Access Token / Refresh Token 구조는 사용하지 않는다.

### 지갑 처리
- 서버에서 새 지갑 생성 기능은 우선 지원하지 않는다.
- 사용자는 MetaMask 등 외부 지갑을 연결하는 방식으로만 진행한다.
- 온체인 기능 실행 전에는 연결된 지갑 확인 및 서명/트랜잭션 승인 확인을 수행한다.

### 민팅
- NFT 민팅은 사용자가 직접 실행하지 않는다.
- 플랫폼 owner 계정이 검수 후 대신 mint 하는 구조로 통일한다.

### 거래 / 상태 관리
- 실제 NFT 소유권 및 거래 완료 여부는 **온체인 기준**으로 판단한다.
- 화면 표시용 상태 및 빠른 조회용 정보는 **DB 캐시 기준**으로 관리한다.

---

## 4. 주요 서비스 로직

## 4.1 회원가입 로직
회원가입은 웹 서비스 계정 생성 과정으로 처리한다.

### 흐름
1. 이메일 중복 확인
2. 비밀번호 해시 처리
3. 사용자 정보 DB 저장
4. 회원가입 완료

### 비고
- 이 단계에서는 지갑 연결을 강제로 수행하지 않는다.
- 지갑 연결은 별도 기능으로 분리한다.

---

## 4.2 로그인 로직
로그인은 세션 기반 인증으로 처리한다.

### 흐름
1. 이메일/비밀번호 입력
2. 사용자 정보 조회
3. 비밀번호 검증
4. 세션 생성
5. 로그인 상태 유지

---

## 4.3 로그아웃 로직

### 흐름
1. 로그아웃 요청
2. 서버 세션 무효화
3. 인증 상태 해제

---

## 4.4 지갑 연결 로직
회원가입/로그인과 별도로 사용자의 외부 지갑을 연결한다.

### 흐름
1. 사용자가 MetaMask 지갑 연결 요청
2. 연결된 지갑 주소 확인
3. Sign Message 요청
4. 지갑 소유자 검증
5. 검증 완료 후 사용자 계정에 지갑 주소 저장

### 비고
- 서버에서 `Wallet.createRandom()` 으로 새 지갑을 생성하는 기능은 현재 지원하지 않는다.
- 지갑 연결은 온체인 기능 실행 전 사전 조건으로 사용한다.

---

## 4.5 온체인 기능 실행 전 지갑 인증 로직
민팅, 판매 등록, 구매, 재판매 등 온체인 동작이 필요한 경우 사전 지갑 검증을 수행한다.

### 대상 기능
- 민팅 요청
- 판매 등록
- 구매
- 재판매

### 흐름
1. 로그인 상태 확인
2. 연결된 지갑 존재 여부 확인
3. 필요 시 지갑 서명 또는 트랜잭션 승인 요청
4. 검증 완료 후 온체인 기능 진행

---

## 4.6 창작물 등록 로직

### 기본 방향
- MVP에서는 **이미지 업로드 중심**으로 진행한다.
- 3D, 음원 등은 추후 확장 가능하도록 고려한다.

### 전체 흐름
1. 이미지 파일 업로드
2. 외부 스토리지에 원본 파일 저장
3. DB에 메타데이터 임시 저장
   - 제목
   - 설명
   - 카테고리
   - 태그
   - 가격
   - 라이선스 범위
   - 미리보기 이미지 또는 샘플 파일 정보
4. 사용자가 내용 확인 및 수정
5. 민팅 요청 단계로 이동

### 역할 분리
- **프론트엔드**: 입력 폼, 파일 선택, 사용자 확인/수정 화면
- **백엔드**: 메타데이터 저장, 상태 관리
- **스토리지**: 원본 파일 저장

---

## 4.7 창작물 검증 로직

### 목적
업로드된 창작물이 기본 조건을 충족하는지 검증한 뒤 민팅 단계로 넘긴다.

### 검증 항목
- 필수 정보 입력 여부
- 파일 형식 검증
- 파일 용량 검증
- 중복 여부 확인
- 사용자 책임 동의 여부 확인

### MVP 기준
MVP에서는 다음 수준으로 단순화한다.
- 기본 형식 검증
- 용량 검증
- 해시 기반 중복 체크
- 사용자 책임 동의

### 비고
- 저작권의 실질적 판단을 시스템이 완전 자동 판별하는 것은 어렵다.
- MVP에서는 관리자 검토와 사용자 책임 동의 중심으로 보완한다.

---

## 4.8 NFT 민팅 로직

### 기본 방향
- 민팅은 플랫폼 owner 계정이 검수 후 대신 실행한다.
- 사용자가 직접 스마트컨트랙트의 mint 함수를 호출하지 않는다.

### 전체 흐름
1. 검수 완료된 창작물 선택
2. NFT 메타데이터 JSON 생성
3. 메타데이터 URI 생성 및 저장
4. 플랫폼 owner 계정이 NFT 컨트랙트의 `mint` 함수 호출
5. 블록체인에 NFT 발행
6. 발행 결과 저장
   - `tokenId`
   - `contractAddress`
   - `txHash`
7. 작품 상태를 `MINTED` 로 변경

### 역할 분리
- **프론트엔드**: 민팅 요청 UI
- **백엔드**: 민팅 요청 처리, 메타데이터 생성, 민팅 결과 저장
- **스토리지**: 메타데이터 JSON 및 원본 파일 저장
- **스마트컨트랙트**: 실제 mint 실행

---

## 4.9 판매 등록 로직

### 기본 방향
민팅된 NFT만 판매 등록할 수 있다.

### 흐름
1. 민팅된 NFT 선택
2. 판매 가격 및 조건 입력
3. 마켓플레이스 컨트랙트에 판매 권한 승인
4. listing 정보 저장
5. 작품 상태를 `LISTED` 로 변경
6. 마켓플레이스 화면에 노출

### 주의점
- 온체인 승인 또는 판매 등록 성공 전에 DB 상태를 먼저 `LISTED` 로 바꾸지 않는다.
- 실제 승인 또는 등록 성공 확인 후 DB 캐시에 반영한다.

---

## 4.10 구매 로직

### 흐름
1. 구매 요청
2. 판매 상태 확인
3. 구매자 지갑 확인
4. 스마트컨트랙트 호출
5. 결제 및 NFT 소유권 이전
6. 거래 내역 저장
7. 작품 상태 변경

### 상태 기준
- **실제 소유권 / 거래 완료 여부**: 온체인 기준
- **화면 표시용 상태**: DB 캐시 기준

### 주의점
- DB 상태를 먼저 바꾸지 않고, 온체인 거래 성공 후 반영한다.

---

## 4.11 재판매 로직

### 흐름
1. 구매자가 보유 NFT를 다시 판매 등록
2. 판매 조건 입력
3. 마켓플레이스 컨트랙트에 판매 권한 승인
4. listing 저장
5. 다른 사용자가 구매
6. 스마트컨트랙트가 자동 정산 수행
   - 원작자 로열티
   - 판매자 수익
   - 플랫폼 수수료

### 전제 조건
이 구조가 성립하려면 마켓플레이스 컨트랙트가 다음 기능을 가져야 한다.
- 로열티 처리 방식 정의
- 플랫폼 수수료율 정의
- 판매 대금 자동 분배 함수 구현

### 현재 방향
- `NexusNFT`: 민팅 및 EIP-2981 로열티 정보 제공
- `NexusMarketplace`: 판매 등록, 구매, 플랫폼 수수료 및 로열티 자동 분배

즉 재판매 자동 정산 구조는 **마켓플레이스 컨트랙트 기준 로직**으로 성립한다.

---

## 5. 스마트컨트랙트 구조 정리

## 5.1 NexusNFT
### 역할
- ERC-721 NFT 발행
- 토큰별 로열티(EIP-2981) 설정
- 플랫폼 owner만 민팅 가능
- 민팅 후 marketplace 승인 구조 지원

### 현재 방향
- 검수 완료 후 플랫폼 owner가 creator 대신 mint
- creator는 민팅된 NFT의 최초 소유자
- 로열티는 creator 기준으로 설정

---

## 5.2 NexusMarketplace
### 역할
- NFT 판매 등록
- 판매 취소
- 구매 처리
- 플랫폼 수수료 / 로열티 / 판매자 수익 자동 분배

### 현재 방향
- `buy()` 에서 다음 정산 수행
  - 플랫폼 수수료
  - EIP-2981 로열티
  - 나머지 판매자 지급

### 주의할 점
- listing 등록 시 marketplace 승인 여부 검증이 필요할 수 있음
- 실제 판매 등록 권한을 `onlyOwner` 로 둘지, 사용자가 직접 하도록 열지 정책 결정 필요
- 구매 성공 후 DB 캐시에 거래 결과를 반영해야 함

---

## 6. 백엔드 설계 방향

## 6.1 현재 백엔드 상태
- Spring Initializr로 Maven 기반 프로젝트 생성 완료
- MariaDB 연결 완료
- 아직 실제 엔티티/리포지토리/API 미구현

## 6.2 권장 패키지 구조
```text
com.kdt.be
├─ common
├─ auth
├─ user
├─ wallet
├─ artwork
├─ review
├─ nft
├─ marketplace
└─ admin